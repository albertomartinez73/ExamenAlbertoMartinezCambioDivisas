@model IEnumerable<ExamenAlbertoMartinezCambioDivisas.Models.ViewModel.ListadoPorSkuVM>

@{
    ViewBag.Title = "Index";
}

<h2>Listado Agrupado Por Sku</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Sku)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SumaAmounts)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Moneda)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Sku)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.SumaAmounts)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Moneda)
    </td>
    <td>
        @Html.ActionLink("Ver Listado Por Sku", "VerTransacciones", new { sku = item.Sku })
        @*@Html.ActionLink("Edit", "Edit", new { id=item.Sku }) |*@
        @*@Html.ActionLink("Details", "Details", new { id=item.Sku }) |*@
        @*@Html.ActionLink("Delete", "Delete", new { id=item.Sku })*@
    </td>
</tr>
}

</table>
